---
import GithubLoginButton from '@components/GithubLoginButton.astro';
import Layout from '@layouts/Layout.astro';
---

<Layout title="Login - Movie Tracker">
	<main>
		<div class="login-container">
			<h1>Bienvenido a <span class="text-gradient">Movie Tracker</span></h1>
			<p>Por favor inicia sesión para continuar.</p>
      <div class="flex flex-col gap-4 items-center">
        <GithubLoginButton />
      </div>
		</div>
	</main>
</Layout>

<style>
	main {
		margin: auto;
		padding: 1rem;
		width: 100%;
		height: 100vh;
		max-width: 1200px;
		color: white;
		font-size: 20px;
		line-height: 1.6;
		display: flex;
		align-items: center;
		justify-content: center;
	}

	.login-container {
		text-align: center;
		padding: 2rem;
		background-color: #23262d;
		border-radius: 10px;
		box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
	}

	h1 {
		font-size: 2.5rem;
		font-weight: 700;
		line-height: 1;
		margin-bottom: 1rem;
	}

	p {
		color: #a1a1aa;
		margin-bottom: 2rem;
	}

	.text-gradient {
		background-image: var(--accent-gradient);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-size: 400%;
		background-position: 0%;
	}
</style>